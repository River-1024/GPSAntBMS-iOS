# 安全策略

## 支持范围

当前只维护 `main` 分支的最新版本。旧提交不会单独发布安全修复。

## 报告安全问题

请使用 GitHub 仓库的 **Security > Report a vulnerability** 私下报告。不要在公开 Issue、Pull Request、Actions 日志或讨论区中提交以下内容：

- 可用的 Token、密码、私钥、证书或描述文件；
- 可识别的真实 GPS 轨迹、设备标识、录像、声音或软件日志；
- 尚未修复漏洞的完整利用步骤。

报告中请说明影响范围、复现条件、受影响版本和建议修复方式。维护者确认问题前，请避免公开披露。

## 仓库安全边界

- CI 只构建未签名 IPA，不需要 Apple 证书或描述文件。
- Pull Request 中的工作流使用只读仓库权限。
- 项目不应包含生产凭据、真实用户数据或私有基础设施地址。
- 若凭据曾进入 Git 历史或 Actions 日志，应立即在提供方撤销或轮换；仅删除文件不能使凭据重新安全。
